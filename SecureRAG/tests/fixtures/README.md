# `tests.fixtures` — Fixtures

> > ⚠️ Part of an **intentionally vulnerable** application. Local lab only — see `docs/LAB_SAFETY.md`.
> **Scaffold only** — Phase 1 creates structure, not behaviour.
> **Status:** scaffold only — Phase 1 creates structure, not behaviour.

## Purpose

Sample documents, recorded model responses, and shared setup.

## Responsibilities

- Small synthetic PDFs for ingestion tests.
- Recorded Ollama responses so tests do not need a live model.

## Files that will exist here later

- `sample_docs/`
- `recorded_responses/`
- `conftest.py`

## This folder must NEVER contain

- Real personal data, real credentials, or real company documents.
